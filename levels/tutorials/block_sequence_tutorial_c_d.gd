extends BlockSequenceTutorial

func _init_texts() -> void:
	_texts.append_array([
		"Press {C_note} to jump and hit the C block",
		"Hit the C block twice to replay the sequence",
		"Replay the C, C, D sequence",
		"Replay the sequence",
	])
